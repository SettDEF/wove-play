/** Build-time feature flags. Flip a flag and rebuild to enable/disable a whole feature across the app. */

/** The audio visualizer (studio, fullscreen, player viz-view, viz background). OFF for the beta release;
 *  set back to `true` to bring it back everywhere it's gated. */
export const SHOW_VISUALIZER = false;
